import React from "react";
import ReactDOM from "react-dom";
import Consumer from "./Consumer";
import "./styles.css";

const rootElement = document.getElementById("root");
ReactDOM.render(<Consumer />, rootElement);
